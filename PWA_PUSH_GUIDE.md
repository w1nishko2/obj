# 📱 Push-уведомления в PWA приложении

## ✅ Да, уведомления работают в установленном PWA!

И работают они **ЛУЧШЕ**, чем в обычном браузере:

### 🎯 Преимущества PWA:

| Платформа | Работает? | Особенности |
|-----------|-----------|-------------|
| **Android** | ✅ ДА | Как нативное приложение! Даже когда PWA закрыто |
| **iOS 16.4+** | ✅ ДА | Только в установленном PWA |
| **Windows** | ✅ ДА | Системные уведомления, работают в фоне |
| **macOS** | ✅ ДА | Системные уведомления |
| **Linux** | ✅ ДА | Системные уведомления |

---

## 📲 Как установить PWA:

### На Android (Chrome/Edge):

1. Откройте сайт в Chrome
2. Нажмите **⋮** (меню) → **"Добавить на главный экран"** или **"Установить приложение"**
3. Подтвердите установку
4. Иконка появится на рабочем столе
5. Разрешите уведомления при первом запуске

**После установки:**
- ✅ Работает как обычное приложение
- ✅ Push приходят даже когда приложение закрыто
- ✅ Показываются в шторке уведомлений Android
- ✅ Можно настроить приоритет, звук, вибрацию

### На iOS (Safari 16.4+):

1. Откройте сайт в Safari
2. Нажмите **[🔗]** (Поделиться) внизу
3. Выберите **"На экран «Домой»"**
4. Подтвердите добавление
5. При первом запуске PWA разрешите уведомления

**Важно для iOS:**
- ⚠️ Работает только в Safari (не в Chrome iOS)
- ⚠️ Требуется iOS 16.4 или новее
- ⚠️ Push работают ТОЛЬКО в установленном PWA, не в Safari

### На Windows (Chrome/Edge):

1. Откройте сайт в Chrome/Edge
2. В адресной строке справа появится иконка **[⊕]** "Установить"
3. Нажмите на неё
4. Подтвердите установку
5. Приложение откроется в отдельном окне

**После установки:**
- ✅ Появится в меню Пуск
- ✅ Можно закрепить на панели задач
- ✅ Push работают как в обычном приложении
- ✅ Уведомления в центре уведомлений Windows
- ✅ Работает даже когда окно PWA закрыто!

### На macOS (Chrome/Edge/Safari):

1. Откройте сайт
2. В меню браузера: **"Установить [название]"**
3. Подтвердите установку
4. Приложение появится в Launchpad

---

## 🧪 Тестирование Push в PWA:

### 1. Установите приложение как PWA

**На компьютере (Chrome):**
```
1. Откройте https://work/push-test
2. Нажмите [⊕] в адресной строке
3. "Установить"
```

### 2. Подпишитесь на уведомления

**В установленном PWA:**
```
1. Откройте PWA
2. Перейдите на /push-test
3. "Подписаться на уведомления"
4. Разрешите уведомления
```

### 3. Закройте PWA полностью

**Важно:** Закройте именно PWA приложение, а не просто браузер!

### 4. Отправьте push

**Из браузера (или другого устройства):**
```
1. Откройте https://work/push-test в обычном браузере
2. Нажмите "🔔 Реальное Push от сервера"
```

### 5. Проверьте результат

**Вы должны увидеть уведомление даже с закрытым PWA!** 🎉

---

## 🔔 Примеры отправки push в PWA:

### Из Laravel кода:

```php
use App\Services\WebPushService;

// При создании задачи
$webPushService->sendToUser(
    userId: $task->assignee_id,
    payload: [
        'title' => 'Новая задача',
        'body' => $task->title,
        'icon' => '/images/icons/task.svg',
        'badge' => '/images/icons/badge.png',
        'data' => [
            'url' => route('tasks.show', $task->id),
            'task_id' => $task->id
        ],
        'actions' => [
            [
                'action' => 'view',
                'title' => 'Открыть'
            ],
            [
                'action' => 'later',
                'title' => 'Позже'
            ]
        ]
    ]
);
```

### При комментарии:

```php
// Уведомить автора задачи
$webPushService->sendToUser(
    userId: $task->user_id,
    payload: [
        'title' => 'Новый комментарий',
        'body' => "{$comment->user->name}: {$comment->text}",
        'icon' => '/images/icons/comment.svg',
        'image' => $comment->photo_url, // Фото из комментария
        'data' => ['url' => route('tasks.show', $task->id)]
    ]
);
```

### Массовая рассылка:

```php
// Всем участникам проекта
$userIds = $project->participants->pluck('id')->toArray();

$webPushService->sendToUsers(
    userIds: $userIds,
    payload: [
        'title' => 'Проект завершен!',
        'body' => "Проект \"{$project->name}\" завершен",
        'icon' => '/images/icons/success.svg',
        'requireInteraction' => true,
        'data' => ['url' => route('projects.show', $project->id)]
    ]
);
```

---

## 📊 Статистика подписок:

```php
// Сколько пользователей подписано на PWA
$pwaSubscriptions = PushSubscription::whereNotNull('user_id')->count();

// Подписки конкретного пользователя
$userSubscriptions = PushSubscription::where('user_id', $userId)->get();

// Удалить старые неактивные подписки (опционально)
PushSubscription::where('updated_at', '<', now()->subMonths(3))->delete();
```

---

## 🎯 Best Practices для PWA Push:

### 1. Персонализация

```php
$notification = [
    'title' => "Привет, {$user->name}!",
    'body' => "У вас {$unreadCount} новых задач",
];
```

### 2. Умные уведомления

```php
// Не отправлять если пользователь онлайн
if (!$user->isOnline()) {
    $webPushService->sendToUser($user->id, $notification);
}
```

### 3. Группировка

```php
// Используйте tag для группировки похожих уведомлений
$notification = [
    'title' => 'Новые сообщения',
    'body' => "У вас {$count} новых сообщений",
    'tag' => 'messages', // Заменит предыдущее уведомление с этим tag
    'renotify' => true   // Снова оповестить
];
```

### 4. Действия в уведомлениях

```php
$notification = [
    'title' => 'Задача просрочена',
    'body' => $task->title,
    'requireInteraction' => true, // Не закроется автоматически
    'actions' => [
        ['action' => 'complete', 'title' => '✅ Выполнить'],
        ['action' => 'postpone', 'title' => '⏰ Отложить'],
        ['action' => 'view', 'title' => '👁 Посмотреть']
    ]
];
```

---

## ⚙️ Настройки PWA для push:

Уже настроено в `manifest.json`:
```json
{
  "permissions": ["notifications", "push"],
  "gcm_sender_id": "103953800507"
}
```

Service Worker уже зарегистрирован в `/service-worker.js`

---

## 🔍 Отладка PWA push:

### Chrome DevTools:

1. Откройте PWA
2. F12 → Application → Service Workers
3. Проверьте статус (должен быть зеленый)
4. Application → Push Messaging → должна быть подписка

### На Android:

1. Подключите телефон к компьютеру
2. В Chrome на ПК: `chrome://inspect/#devices`
3. Найдите ваше PWA → "Inspect"
4. Смотрите логи консоли

### На iOS:

1. Settings → Safari → Advanced → Web Inspector
2. Подключите iPhone к Mac
3. Safari на Mac → Develop → [Your iPhone]
4. Выберите PWA

---

## 📱 Пример: Полный цикл работы

1. **Пользователь устанавливает PWA** на телефон/ПК
2. **Разрешает уведомления** при первом запуске
3. **Закрывает приложение** полностью
4. **На сервере происходит событие** (новая задача)
5. **Laravel отправляет push** через WebPushService
6. **Уведомление приходит** на устройство пользователя
7. **Пользователь нажимает** на уведомление
8. **PWA открывается** на нужной странице

---

## ✅ Выводы:

- ✅ **PWA + Push = как нативное приложение**
- ✅ **Работает на всех платформах**
- ✅ **Независимо от FCM/Firebase**
- ✅ **Уведомления работают в фоне**
- ✅ **Полный контроль со стороны сервера**

**Ваше приложение готово к работе как PWA с push-уведомлениями!** 🚀
