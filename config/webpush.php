<?php

return [
    /*
    |--------------------------------------------------------------------------
    | VAPID Ключи для Web Push
    |--------------------------------------------------------------------------
    |
    | VAPID (Voluntary Application Server Identification) ключи используются
    | для идентификации вашего сервера при отправке push-уведомлений.
    | 
    | Для генерации ключей используйте команду:
    | php artisan webpush:vapid
    |
    */
    'vapid' => [
        'subject' => env('VAPID_SUBJECT', 'mailto:admin@example.com'),
        'public_key' => env('VAPID_PUBLIC_KEY', ''),
        'private_key' => env('VAPID_PRIVATE_KEY', ''),
    ],

    /*
    |--------------------------------------------------------------------------
    | Настройки уведомлений по умолчанию
    |--------------------------------------------------------------------------
    */
    'notification_defaults' => [
        'icon' => '/images/icons/icon.svg',
        'badge' => '/images/icons/badge.png',
        'vibrate' => [200, 100, 200],
        'requireInteraction' => false,
        'renotify' => false,
        'silent' => false,
        'tag' => 'notification',
        'timestamp' => null,
    ],

    /*
    |--------------------------------------------------------------------------
    | Время жизни уведомления (TTL)
    |--------------------------------------------------------------------------
    |
    | Указывает, как долго push-сервис должен хранить уведомление,
    | если устройство недоступно (в секундах).
    | По умолчанию: 4 недели (2419200 секунд)
    |
    */
    'ttl' => env('WEBPUSH_TTL', 2419200),

    /*
    |--------------------------------------------------------------------------
    | Срочность уведомления
    |--------------------------------------------------------------------------
    |
    | Возможные значения: very-low, low, normal, high
    |
    */
    'urgency' => env('WEBPUSH_URGENCY', 'normal'),

    /*
    |--------------------------------------------------------------------------
    | Тема уведомления
    |--------------------------------------------------------------------------
    |
    | Используется для группировки уведомлений на мобильных устройствах
    |
    */
    'topic' => env('WEBPUSH_TOPIC', null),

    /*
    |--------------------------------------------------------------------------
    | Автоматическая очистка недействительных подписок
    |--------------------------------------------------------------------------
    |
    | Если true, недействительные подписки будут автоматически удаляться
    | из базы данных при получении ошибки от push-сервиса.
    |
    */
    'auto_cleanup' => env('WEBPUSH_AUTO_CLEANUP', true),
];
