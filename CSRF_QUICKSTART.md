# 🚀 Быстрый старт: Установка CSRF Token Auto-Refresh

## Шаг 1: Скомпилировать assets

```bash
npm run build
```

Или для разработки с автообновлением:
```bash
npm run dev
```

## Шаг 2: Проверить работу

Откройте в браузере тестовую страницу:
```
http://ваш-домен/csrf-test
```

На этой странице вы можете:
- ✅ Просмотреть текущий токен
- ✅ Вручную обновить токен
- ✅ Протестировать AJAX запросы
- ✅ Симулировать устаревший токен
- ✅ Посмотреть лог событий

## Шаг 3: Включить отладку (опционально)

В файле `resources/js/csrf-refresh.js` измените:

```javascript
window.csrfManager = new CsrfTokenManager({
    debug: true, // Включить отладку
});
```

Затем пересоберите:
```bash
npm run build
```

## Готово! 🎉

Система автоматически работает для всех:
- HTML форм с `@csrf`
- AJAX запросов через Axios
- Любых POST/PUT/PATCH/DELETE запросов

## Настройки по умолчанию

- **Интервал обновления:** 100 минут (сессия по умолчанию 120 минут)
- **Endpoint:** `/refresh-csrf`
- **Автоматическая обработка 419 ошибок:** Включена

## Что дальше?

Подробная документация в файле: `CSRF_TOKEN_REFRESH.md`
