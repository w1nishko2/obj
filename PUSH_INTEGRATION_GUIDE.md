# 🔔 Push-уведомления для участников проекта

## ✅ Что реализовано:

### 📤 Автоматические уведомления отправляются при:

1. **Создании этапа** - все участники проекта узнают о новом этапе
2. **Создании задачи** - уведомление всем участникам о новой задаче  
3. **Добавлении материала** - информация о новых материалах в этапе
4. **Добавлении доставки** - уведомление о запланированной доставке
5. **Добавлении участника** - уведомление существующим участникам + отдельное для нового участника
6. **Добавлении фото** - уведомление с превью фотографии к задаче
7. **Добавлении комментария** - все участники видят новые комментарии

### 🎯 Фильтрация получателей:

- ✅ **Только участники проекта** получают уведомления
- ✅ **Только с подпиской** - пользователи, давшие разрешение на уведомления
- ✅ **Исключение автора** - пользователь не получает уведомление о своем действии

### 🎨 Модальное окно разрешений:

- ✅ Показывается **после первого входа/регистрации**
- ✅ **Красивый дизайн** в стиле проекта с градиентами
- ✅ **Подробное описание** каждого разрешения
- ✅ **Автоматическая подписка** на push при разрешении
- ✅ **Можно пропустить** и включить позже
- ✅ **Запоминает выбор** в localStorage

## 📋 Структура кода:

### Сервис уведомлений:
```
app/Services/ProjectNotificationService.php
```

**Методы:**
- `notifyStageCreated()` - новый этап
- `notifyTaskCreated()` - новая задача  
- `notifyMaterialAdded()` - новый материал
- `notifyDeliveryAdded()` - новая доставка
- `notifyParticipantAdded()` - новый участник
- `notifyPhotoAdded()` - новое фото
- `notifyCommentAdded()` - новый комментарий
- `notifyTaskStatusChanged()` - изменение статуса задачи

### Интеграция в контроллеры:

**ProjectController:**
- `storeStage()` - уведомление при создании этапа
- `addParticipant()` - уведомление при добавлении участника

**StageTaskController:**
- `storeTask()` - уведомление при создании задачи
- `storeMaterial()` - уведомление при добавлении материала
- `storeDelivery()` - уведомление при добавлении доставки
- `addPhoto()` - уведомление при добавлении фото
- `addComment()` - уведомление при добавлении комментария

### Frontend компоненты:

**Модальное окно:**
```
resources/views/components/permissions-modal.blade.php
```

**Web Push Manager:**
```
public/js/webpush-manager.js
```

**Layout интеграция:**
```
resources/views/layouts/app.blade.php
```

## 🎬 Как это работает:

### 1. Первый вход пользователя:

```
Пользователь входит
    ↓
Через 1.5 сек показывается модалка
    ↓
Пользователь нажимает "Разрешить"
    ↓
Браузер запрашивает разрешение
    ↓
Web Push Manager подписывается
    ↓
Подписка сохраняется в БД
```

### 2. Создание задачи:

```
Прораб создает задачу
    ↓
StageTaskController::storeTask()
    ↓
ProjectNotificationService::notifyTaskCreated()
    ↓
Получаем участников проекта (кроме автора)
    ↓
WebPushService::sendToUsers()
    ↓
Push отправляется всем подписанным участникам
    ↓
Уведомление появляется даже при закрытом браузере!
```

### 3. Получение уведомления:

```
Push приходит на устройство
    ↓
Service Worker получает событие
    ↓
Показывает уведомление с иконкой, текстом, действиями
    ↓
Пользователь кликает на уведомление
    ↓
Открывается нужная страница проекта
```

## 🔧 Настройка:

### Все уже настроено! ✅

- ✅ VAPID ключи сгенерированы
- ✅ Сервисы зарегистрированы
- ✅ Контроллеры интегрированы
- ✅ Модалка добавлена в layout
- ✅ Service Worker обновлен

### Что проверить:

1. **Миграция выполнена:**
   ```bash
   php artisan migrate
   ```

2. **VAPID ключи в .env:**
   ```env
   VAPID_PUBLIC_KEY=...
   VAPID_PRIVATE_KEY=...
   VAPID_SUBJECT=mailto:your-email@example.com
   ```

3. **Очистить кэш:**
   ```bash
   php artisan config:clear
   php artisan cache:clear
   ```

## 🧪 Тестирование:

### Сценарий 1: Проверка модального окна

1. Откройте сайт в **режиме инкогнито** (чтобы сбросить localStorage)
2. Войдите в систему
3. Через 1.5 сек должно появиться модальное окно
4. Нажмите "Разрешить уведомления"
5. Браузер запросит разрешение - разрешите
6. Должно появиться сообщение "Уведомления успешно включены!"

### Сценарий 2: Проверка уведомлений в проекте

1. **Пользователь A** (Прораб) и **Пользователь B** (Участник) подписаны на уведомления
2. Оба в одном проекте
3. **Пользователь A создает задачу**
4. **Пользователь B получает push-уведомление** "✅ Новая задача"
5. **Пользователь A НЕ получает** (он автор)

### Сценарий 3: Проверка с закрытым браузером

1. Подпишитесь на уведомления
2. **Закройте браузер полностью**
3. Попросите кого-то добавить комментарий к задаче
4. **Получите уведомление** даже с закрытым браузером!
5. Кликните - браузер откроется на нужной странице

## 📱 Мобильные устройства:

### Android:
- ✅ Работает идеально
- ✅ Уведомления в шторке
- ✅ Работает в фоне

### iOS:
- ✅ Работает в PWA (iOS 16.4+)
- ⚠️ Требуется установка как PWA
- ⚠️ Только в Safari

## 🐛 Отладка:

### Проверить подписки:
```sql
SELECT * FROM push_subscriptions;
```

### Проверить логи:
```bash
tail -f storage/logs/laravel.log
```

### Консоль браузера:
```javascript
// Проверить подписку
navigator.serviceWorker.ready.then(reg => {
  reg.pushManager.getSubscription().then(sub => {
    console.log('Подписка:', sub);
  });
});

// Проверить разрешение
console.log('Permission:', Notification.permission);

// Проверить localStorage
console.log('Asked:', localStorage.getItem('permissionsAsked'));
console.log('Granted:', localStorage.getItem('permissionsGranted'));
```

## 💡 Дополнительные возможности:

### Отправить уведомление вручную:

```php
use App\Services\ProjectNotificationService;

$notificationService = app(ProjectNotificationService::class);

$notificationService->notifyTaskCreated(
    $project,
    $stage,
    $task,
    Auth::user()
);
```

### Отправить кастомное уведомление:

```php
use App\Services\WebPushService;

$webPushService = app(WebPushService::class);

// Получаем ID участников проекта
$participantIds = $project->participants()->pluck('user_id')->toArray();

$webPushService->sendToUsers(
    userIds: $participantIds,
    payload: [
        'title' => 'Кастомное уведомление',
        'body' => 'Текст уведомления',
        'icon' => '/images/icons/icon.svg',
        'data' => ['url' => '/some-page']
    ]
);
```

## 🎉 Готово!

Система полностью работает и интегрирована во все нужные места. Пользователи будут получать уведомления о всех важных событиях в проектах!

**Следующие шаги:**
1. Протестируйте модальное окно
2. Создайте задачу и проверьте уведомления
3. Установите PWA на мобильное устройство
4. Наслаждайтесь! 🚀
